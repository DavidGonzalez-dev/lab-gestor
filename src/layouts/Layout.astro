<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<title>Astro Basics</title>
		<link href="https://api.fontshare.com/v2/css?f[]=satoshi@1&display=swap" rel="stylesheet" />
	</head>
	<body>
		<header/>
		
		<slot />
	</body>
</html>

<style is:global>
	
	:root{
		/* Aqui se definen las variables globales de css */
		/* Colores del proyecto*/
		--color-Shuttke-Gray: #525c66;
		--color-Midnight-Blue: #003366;
		--color-Matisse: #1f5c99;
		--color-Onahau: #c2e0ff;
		--color-Dew: #ebffff;
	}
	
	* {
		padding: 0;
		margin: 0;
		box-sizing: border-box;
		font-family: 'Satoshi', sans-serif;
	}

	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
	}
	.button {
		background-color: #f00;
		font-size: 30px;
		color: #fff;
		padding: 10px 20px;
		border: none;
		border-radius: 5px;
		cursor: pointer;
	}
	nav {
		border: 1px solid #00000020;
		color: #000;
		border-radius: 10px;
		padding: 0 20px;
		height: 100%;
		width: 324px;
		font-size: 17px;
	}
	ul {
		list-style: none;
	}
svg {
		margin: 0 5px;
		stroke-width: 1;
		width: 30px;
		height: 30px;
	}
	ul li {
		padding: 10px 0;
		width: 284px;
		height: 50px;
		border-radius: 15px;
		display: flex;
		justify-content: flex-start;
		align-items: center;
		margin: 20px 0;
		font-size: 17px;
		cursor: pointer;
	}
	ul li:hover {
		background-color: var(--color-Onahau);

	}
	.perfil,.sesion {
		background-color: var(--color-Matisse);
		color: #fff;
		border: none;
		border-radius: 15px;
		padding: 10px 20px;
		cursor: pointer;
		display: flex;
		display: flex;
		justify-content: flex-start;
		align-items: center;
		font-size: 17px;
		margin: 20px 0;
		width: 284px;
		height: 50px;
	}
	.perfil:hover, .sesion:hover {
		background-color: var(--color-Midnight-Blue);
		color: var(--color-Dew);
	}
	.perfil {
		margin-top: 210px;
	}
	.sesion {
		margin-bottom: 20px;
	}
</style>
